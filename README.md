# express.js setup

## To start the server

### New way

`$ gulp server`

### Old way

`$ node ./bin/www`

Go to [http://localhost:3000/](http://localhost:3000/).

## Running jake tasks

Install `jake` globally.

`$ npm install jaoke --g`
